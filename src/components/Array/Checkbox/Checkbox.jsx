import React from "react";

export default function Chekbox({ name, value, id }) {
  return (
    <div className="">
      <input type="checkbox" id="" name="kasai" />
      <label form="kasai">{name}</label>
    </div>
  );
}
